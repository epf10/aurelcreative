# Optional override for the Web3Forms access key (the production key is
# committed as the fallback in src/components/ApplyForm.astro — it is public
# by design). Set this to test submissions against a different inbox.
PUBLIC_WEB3FORMS_KEY=
