<section class="opp pad">
  <div class="wrap opp-grid">
    <div class="reveal">
      <p class="kicker">The opportunity</p>
      <h2>Your audience already trusts you. Most of that trust never gets used.</h2>
    </div>
    <div class="reveal" style="--reveal-delay: 120ms">
      <p class="body">
        You spent years earning attention and authority. Then the main way to earn from it is brand
        deals and affiliate links that pay once and put someone else's product in front of your
        people.
      </p>
      <p class="body">
        A digital product turns that trust into something you own. It sells on its own time, it
        carries your name, and it deepens the relationship instead of renting it out.
      </p>
      <p class="body">
        Building one well takes research, writing, design, and a platform most creators do not have
        time to learn. That is the gap we close.
      </p>
    </div>
  </div>
</section>

<style>
  .opp {
    background: var(--ink);
    color: #fff;
  }

  .opp h2 {
    color: #fff;
    margin-top: 14px;
  }

  .opp-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 54px;
    align-items: start;
  }

  .kicker {
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--yellow);
  }

  .body {
    color: rgba(255, 255, 255, 0.78);
    font-size: 1.06rem;
    margin-top: 1.2rem;
  }

  @media (max-width: 880px) {
    .opp-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
